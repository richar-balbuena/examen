name: examen
description: Aplicación unificada con Mensajería y Calculadora
publish_to: 'none' # No se publica en pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 📩 Dependencias para el módulo de Mensajería
  provider: ^6.1.1
  dio: ^5.4.0
  cached_network_image: ^3.3.1
  intl: ^0.18.1

  # Dependencias para el módulo de Calculadora
  get: ^4.6.6

  # 🎨 Íconos de estilo iOS (usados por defecto en Flutter)
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # 📸 Imágenes o recursos que usa la aplicación
  assets: []
